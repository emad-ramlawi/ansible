---
- hosts: all
  become: yes
  tasks:

  - name: apt-get clean all -y
    shell: apt-get clean all -y
      
  - name: sudo apt-get autoclean -y 
    shell: sudo apt-get autoclean -y 
    
  - name: sudo apt-get autoremove -y
    shell: sudo apt-get autoremove -y
    
  - name: apt install -f
    shell: apt install -f
    
  - name: find /tmp -type f -atime +1 -delete
    shell: find /tmp -type f -atime +1 -delete
